# Optional: default input CSV (demo uses the bundled sample files if unset)
# REORDER_INPUT=data/my_orders.csv

# Output directory for digest.txt, alerts.csv, errors.csv (default: ./output)
# REORDER_OUTPUT=output

# For a real scheduled run, set REORDER_INPUT to your ERP export path
# and call this script from cron or an n8n Schedule Trigger node.
